{% extends 'base.html.twig' %}

{% block title %}Edit Commentaire{% endblock %}

{% block body %}
    <h1>Edit Commentaire</h1>

    {{ include('comments/_form.html.twig', {'button_label': 'Update'}) }}
    <br>
    {{ include('comments/_delete_form.html.twig') }}
    <table>
        <tr>
            <td>
                <a href="{{ path('comments_all') }}">back to list</a>
            </td>
        </tr>
    </table>
{% endblock %}
