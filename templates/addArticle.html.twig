{% extends "base.html.twig" %}

{% block body %}

    <h1> Add one article </h1>

    {{ form_start(form) }}

    {{ form_row(form.title, {
        'label': "Titre de l'articles",
        "attr": {
            'placeholder': "Entrez le titre de l'articles"
        }
    }) }}

    {{ form_row(form.photo, {
        'label': "Photo de l'articles (Pas obligatoire)",
        "attr": {
            'placeholder': "Entrez le lien de la photo"
        }
    }) }}

    {{ form_row(form.description, {
        'label': "Description de l'articles",
        "attr": {
            'placeholder': "Description de l'articles"
        }
    }) }}

    {{ form_row(form.save) }}

    {{ form_end(form) }}

{% endblock %}